/**
 * install jsonwebtoken
 * jwt.sign(payload, secret, {expiresIn:})
 * token client
 * 
 */

/**
 * how to store token in the client side
 * 1. memory --> ok type
 * 2. localStorage --> ok type (XSS: cross site scripting)
 * 3. cookies --> http only
 * 
 */